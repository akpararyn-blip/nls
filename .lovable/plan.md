## Главная — секция «Услуги»

Сейчас на главной 4 карточки услуг + CTA. В проекте есть полный набор страниц-услуг (см. `src/config/links.ts`): интернет, IT-аутсорсинг, СКС, colocation, dedicated, full rack, VPS, IaaS, облако, object storage, cloud storage. Многие из них не представлены отдельными карточками.

Переработать секцию `Services` в `src/routes/index.tsx`:

- Сгруппировать в 3 блока со своими подкарточками:
  1. **Связь и сети** — Интернет для бизнеса, СКС, IP/мониторинг (ссылки на `/internet`, `/it-sks`).
  2. **IT-инфраструктура** — IT-аутсорсинг, статьи-руководства из it-sks (`/vybor-oborudovaniya-dlya-lvs`, `/oshibki-pri-montazhe-sks`, `/kak-pravilno-prolozhit-kabel`).
  3. **Дата-центр и облака** — Colocation, Full rack, Dedicated, VPS, IaaS, Cloud, Object Storage, Cloud Storage.
- Каждая подуслуга — компактная карточка с заголовком, кратким описанием и ссылкой через `SmartLink` (учёт поддоменной маршрутизации сохраняется).
- Сохранить финальную CTA-карточку «Не знаете, что выбрать?».
- Стили: добавить новые классы в `src/styles/nls.css` (сетка групп, мини-карточки, hover, иконка-стрелка).
- Двуязычные подписи (ru/kz) через `useT`, в стилистике существующих текстов.

## Главная — мелкие штрихи

- В `Trust` добавить кратко-метрики (18+ лет, 15 000+ клиентов, 70 шкафов, 24/7 SLA) если визуально пусто после расширения услуг — оценить по верстке, не раздувать.
- Без изменений в Hero, DataCenter, UseCases, FinalCTA.

## «О компании» — hero

Текущий `about-hero` — текст + статы на плоском фоне. Переработать визуально, без изменения смысла:

- Двухколоночная композиция (на десктопе): слева eyebrow + крупный H1 с акцентом «NLS» и градиентным подчёркиванием ключевого слова, лид-параграф, CTA «Получить консультацию» и вторичная ссылка «Наши услуги» → якорь к `about-services`.
- Справа — декоративная визуальная панель: карточка-«мозаика» из 4 статов (стекло/blur, тонкая граница, мягкое свечение оранжевого), либо изображение `datacenter.png` с overlay-чипами (18+ лет, Tier III, 24/7, 18 000 км). Выбираю мозаику статов — без новых ассетов, согласуется с цветовой схемой.
- Фон секции: тёмная подложка с тонкой сеткой/радиальным glow в фирменном оранжевом (через css-градиенты, без новых файлов).
- На мобильном — стек: заголовок, текст, CTA, ниже плитка статов 2×2.
- Стили в `src/styles/nls.css`: переписать `.about-hero`, `.about-hero-title`, добавить `.about-hero-grid`, `.about-hero-visual`, `.about-hero-stats-card`, `.about-hero-actions`.

## Технические правки

- `src/routes/index.tsx` — переписать `Services()`, импортировать `SmartLink`.
- `src/routes/about.tsx` — переписать JSX секции `about-hero` (двухколоночник, CTA-блок, перенос статов в визуал).
- `src/styles/nls.css` — новые/обновлённые классы (без затрагивания других секций).
- Без изменений: бизнес-логика, формы, blacklist, конфиг links, маршруты.

## Что НЕ делаем

- Не трогаем картинки/ассеты (не добавляем новых файлов).
- Не меняем поддоменную маршрутизацию.
- Не редизайним остальные секции главной и about.
